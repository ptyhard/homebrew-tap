# homebrew-tap

ptyhard のツールを配布する [Homebrew](https://brew.sh/) tap。

## インストール

```bash
brew install ptyhard/tap/<formula>
```

formula / cask は各プロジェクトの [GoReleaser](https://goreleaser.com/) によってリリース時に自動生成・push される。
